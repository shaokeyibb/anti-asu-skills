# 贡献指南

欢迎 Issue 和 PR。这个项目的规则会影响真实的招聘决策，所以贡献时有几条比代码规范更重要的东西。

## 先读这一段：这个项目最容易走错的方向

**加严一条规则的成本，几乎总是比它的收益更隐蔽。**

每加一条"这可能是问题"的判据，都会同时提高**漏检率的下降**和**误伤率的上升**。而误伤是不可见的——被误判的候选人不会回来告诉你他被冤枉了。

所以本项目的默认倾向是：**宁可漏报，不可错杀**。提交规则时请自问：

- 这条规则会不会因为一个正当的原因而命中？（几乎总是会——把那个原因写进"必须排除"）
- 它的结论上限应该是什么档？（大多数规则的上限是 `部分属实` 或 `疑似`，能到 `直接矛盾` 的极少）
- 它会不会对某类人群系统性不利？（例如惩罚低活跃度会不利于代码在公司内网的工程师）

## 新增识别规则的格式

每条规则至少包含四部分：

```markdown
### X.Y 规则名 —— 权重：低/中/高

**形态**：这个手法长什么样，给一个具体例子。

**为什么是信号**：解释机制，不要只说"这看起来可疑"。

**必须排除**：
- 正当解释 1
- 正当解释 2
- ...

**结论上限**：`部分属实` / `疑似` / ...，以及为什么不能更高。
```

**"必须排除"一节不能为空。** 如果你想不出任何正当解释，说明这条规则还没想清楚。

## 两个最欢迎的贡献方向

### 项目类型难度基准表

文件：`skills/project-check/references/boilerplate-catalog.md`

遇到未收录的高频项目类型时，按四段补充：

- **识别特征**：技术栈组合、典型目录结构；
- **典型实现范围**：跟着主流教程做通常会做到什么程度；
- **超出范围的标志**：什么内容说明候选人做了远超模板的工作（**这是加分信号，要记录**）；
- **需要追问的描述**：哪些简历表述的强度明显超出典型范围。

**务必保持基调**：这是**难度基准表，不是黑名单**。做常见项目完全正当，且往往是最有效的学习路径。这份清单唯一的用途是**对照简历描述的强度是否超出了该类项目的实际难度**。

### 技术方向深水区清单

文件：`skills/grill/references/question-design-patterns.md`

新增一个技术方向时，请提供一道**完整的示例题**，包含：

- 题面（可直接念给候选人）；
- 埋的坑是什么，以及为什么只学过表面的人会掉进去；
- 参考答案要点（可勾选）；
- 典型的错误回答；
- **答不上来不说明什么**。

深水区的通用规律：**教程讲 happy path，实践的痛苦全在异常路径上**——超时、重试、并发、故障、数据不一致、边界值。

## 绝对不接受的内容

- **任何具体真人的信息**：姓名、账号、PR 编号、出生日期、就读院校、公司内部信息。所有示例必须是虚构的，或以通用形态书写。
- **基于受保护属性的推断规则**：年龄、性别、地域、院校层次、就业空窗、婚育状况。即使推理链看起来严谨——尤其是看起来严谨的时候。
- **"AI 生成内容检测"类规则**。AI 检测在原理上不可靠，且 AI 辅助写作是正当的标准实践。
- **贬低某类工作的规则**：前端、文档、测试、i18n 都是正当且必要的工程方向。
- **可用于淘汰候选人的批量筛查逻辑**。`/pipeline-pattern` 的产出只能是提问策略建议。

## 提交前

```bash
python3 scripts/validate_skills.py
```

校验器会检查 frontmatter、目录名一致性、跨文件引用路径，以及**隐私红线**（手机号、真实邮箱形态）。

如果你的改动涉及核验行为，建议同时更新 `evals/evals.json`：

- 新增能力 → 加一条**灵敏度**断言（能不能抓到）；
- 新增护栏 → 加一条**特异性**断言（会不会误伤），并加 `【纪律】` 或 `【反歧视】` 前缀。

## 提交规范

使用 Conventional Commits：

```text
feat(project-check): 新增「向量数据库」项目类型基准
fix(blog-check): 修正样本量阈值在 100-300 区间的表述
docs(readme): 补充七档分级的说明
```

## 一件请记住的事

这套工具的产出会影响一个人能不能拿到工作。

报告写给面试官看，但**请假设候选人有一天会读到它**。陈述事实，不做人身评价；对候选人真实的能力和贡献，同样如实记录。

核验报告不是找茬报告。
